'use client';
import {FormEvent,useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';import {api} from '@/lib/api';
const demos=[['Administrador','admin@inventoryflow.demo'],['Supervisor','supervisor@inventoryflow.demo'],['Operador','operator@inventoryflow.demo']];
export default function Login(){const[email,setEmail]=useState('admin@inventoryflow.demo');const[password,setPassword]=useState('Demo123!');const[err,setErr]=useState('');const[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setLoading(true);setErr('');try{await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});router.push('/dashboard')}catch(e:any){setErr(e.message)}finally{setLoading(false)}}return <main className="loginPage"><section className="loginAside"><Link href="/" className="landingBrand"><span>IF</span><strong>Inventory Flow</strong></Link><div><span className="heroKicker">DEMONSTRAÇÃO DO SISTEMA</span><h1>Explore uma operação de inventário completa.</h1><p>Catálogo, usuários, contagens e divergências utilizam dados fictícios preparados para demonstração.</p></div><small>Ambiente independente de dados empresariais e credenciais externas.</small></section><section className="loginCard"><form onSubmit={submit}><span className="eyebrow">ACESSO À DEMONSTRAÇÃO</span><h2>Entrar no sistema</h2><p>Use um dos perfis abaixo para conhecer as permissões.</p>{err&&<div className="alert error">{err}</div>}<label>E-mail<input value={email} onChange={e=>setEmail(e.target.value)} type="email"/></label><label>Senha<input value={password} onChange={e=>setPassword(e.target.value)} type="password"/></label><button className="primary loginButton" disabled={loading}>{loading?'Entrando...':'Entrar'}</button><div className="demoAccounts">{demos.map(x=><button type="button" key={x[1]} onClick={()=>{setEmail(x[1]);setPassword('Demo123!')}}><strong>{x[0]}</strong><span>{x[1]}</span></button>)}</div><small className="demoPassword">Senha dos perfis de demonstração: <b>Demo123!</b></small></form></section></main>}
